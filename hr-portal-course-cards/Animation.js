// FadeIn (300ms) - плавное появление (изменение opacity)

// ScaleUp для контента - увеличение содержимого попапа (transform: scale)

// Комбинированная анимация - одновременное выполнение двух эффектов

// Обработчики - реакция на mouseenter и click

// Оптимизация - использование requestAnimationFrame


function animatePopup(element, startProps, endProps, duration) {
   let startTime = null;

   function animateFrame(timestamp) {
        
   }
}